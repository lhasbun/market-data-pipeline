symbols:
  - AAPL
  - MSFT
  - TSLA

data_dir: "data/"

providers:
  priority:
    - yahoo
    - alpha_vantage

ingestion:
  start_date: "2015-01-01"
  end_date: null        # null = up to today
  retry_attempts: 3
  retry_delay_seconds: 2

logging:
  level: "INFO"
